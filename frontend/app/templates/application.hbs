<div class="row">
  <div class="margin-top">
    <h1 id="title">Bluebird Feeder</h1>
  </div>
  <br>
  <p>
    {{#if controllers.session.currentUser}}
      Hi, {{controllers.session.currentUser.name}}
    {{else}}
      Hello, Guest
    {{/if}}
  </p>
</div>

  {{outlet}}

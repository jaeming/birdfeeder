import {
  autocomplete
} from 'frontend/helpers/autocomplete';

module('AutocompleteHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = autocomplete(42);
  ok(result);
});
